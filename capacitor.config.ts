import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sveltetechassessmentphelo.app',
  appName: 'wyzetalk-assessment',
  webDir: 'dist'
};

export default config;
